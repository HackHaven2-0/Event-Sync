const Event = require("../../../model/eventModel.js");

// Mock database

const createEvent = async (req,res)=>{
    
}

module.exports = {
    createEvent,
    getAllEvents,
    getEventById,
    updateEvent,
    deleteEvent,
};